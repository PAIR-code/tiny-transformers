For the logic docs; actually, instead of trying to get the escaping right in the HTML, lets write a little HTMLify for our logic code that uses our syntax highlighting? (lets make a test first that this will produce the HTML we want it to, e.g. some of the stuff that's in the HTML file now?) Then we can use real code in the examples? Maybe we can even use monaco editor component, and let the docs be editable: lets make some dedicated component to explain the logic code, using monaco, and having a reset button (to get back to the starting value, and maybe an execute button too)... Make sure to re-read the logic explorer code, and make some appropriate re-usable components too.
